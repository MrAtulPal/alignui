{
  "name": "designlatch",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "test": "jest --runInBand",
    "typecheck": "npm -ws run typecheck"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "tsup": "^8.5.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.0"
  }
}
